<div class="stock-widget {$TickStatus}">
    <span class="symbol">{$Symbol}</span>
    <span class="last-price">{$LastPrice.Nice}</span>
    <span class="previous-close">{$PreviousClose.Nice}</span>
    <span class="last-updated">{$LastUpdated.Nice}</span>
</div>